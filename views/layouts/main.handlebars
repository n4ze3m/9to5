<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="The most productive hours of the day, now in countdown form!"
    />
    <meta name="theme-color" content="#FFDE3D" />
    <meta name="robots" content="follow, index, work-from-9-to-5" />
    <meta name="mood" content="Professional during business hours only" />
    <meta name="coffee-level" content="Highly caffeinated" />
    <meta name="productivity" content="Over 9000" />
    <meta name="overtime-policy" content="Error 418: I'm a teapot after 5pm" />
    <title>9 to 5 Website</title>
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;700&display=swap"
    />
    <style>
      body { background-color: #FFDE3D; font-family: 'Space Grotesk',
      sans-serif; display: flex; flex-direction: column; align-items: center;
      justify-content: center; min-height: 100vh; margin: 0; color: #000000;
      padding: 20px; position: relative; overflow-x: hidden; } /* Cool geometric
      shapes in background */ body::before { content: ''; position: fixed; top:
      20px; right: 20px; width: 60px; height: 60px; background: #FF3D77; border:
      3px solid black; transform: rotate(15deg); z-index: -1; } body::after {
      content: ''; position: fixed; bottom: 20px; left: 20px; width: 40px;
      height: 40px; background: #4AFF3D; border: 3px solid black; transform:
      rotate(-10deg); z-index: -1; } .container { background-color: #ffffff;
      padding: min(5vw, 3rem); border: 3px solid #000000; border-radius: 2px;
      box-shadow: 12px 12px 0px #000000; text-align: center; transform:
      rotate(-1deg); width: min(95%, 800px); margin: 2rem auto; position:
      relative; transition: transform 0.3s ease; } .container:hover { transform:
      rotate(1deg) translateY(-5px); } h1 { font-size: clamp(2rem, 6vw, 4rem);
      font-weight: 700; margin-bottom: 2rem; text-transform: uppercase; color:
      #FF3D77; text-shadow: 3px 3px 0px #000000; } #counter { font-size:
      clamp(1.8rem, 4vw, 2.5rem); font-weight: 700; background-color: #4AFF3D;
      padding: 1rem 2rem; border: 3px solid #000000; box-shadow: 6px 6px 0px
      #000000; margin: 1.5rem 0; transform: rotate(1deg); display: inline-block;
      transition: transform 0.2s ease; } #counter:hover { transform: scale(1.05)
      rotate(-1deg); } p { font-size: clamp(1rem, 2vw, 1.2rem); font-weight:
      500; text-transform: uppercase; letter-spacing: 1px; border-bottom: 3px
      solid #000000; padding-bottom: 1rem; margin: 1rem auto; width:
      fit-content; } .quote { font-style: italic; margin-top: 2rem; font-size:
      clamp(0.9rem, 1.5vw, 1.1rem); color: #666; background: #f3f3f3; padding:
      1rem; border-left: 5px solid #FF3D77; } /* Responsive adjustments */
      @media (max-width: 768px) { .container { width: 90%; box-shadow: 8px 8px
      0px #000000; } } @media (max-width: 480px) { .container { transform: none;
      width: 85%; box-shadow: 6px 6px 0px #000000; padding: 1.5rem; } #counter {
      transform: none; box-shadow: 4px 4px 0px #000000; } body::before,
      body::after { width: 30px; height: 30px; } }
    </style>
  </head>
  <body>
    {{{body}}}
  </body>
</html>